# Figma 节点缓存 — My Files Tab 区域

- 文件 fileKey：`f7bHkVNYG1X14LwsMgkPgx`
- 节点 nodeId：`0:1755`（图层名 `编组 6`，类型：文件列表）
- 来源链接：https://www.figma.com/design/f7bHkVNYG1X14LwsMgkPgx/...?node-id=0-1755
- 读取时间：2026-05-12
- 资源策略：按全局 Figma 约束，仅记结构与 token，不缓存图片远程 URL，也不主动下载

## 结构概览

```
编组 6 — My Files 列表容器 (904 × 508, bg #242426, 圆角 8)
├── 列表项 01 (904 × 96, top=156, bg #242426)            ← 默认态
├── 列表项 01备份 (904 × 96, top=252, bg #2a2a2c)         ← hover 态
└── 列表项 01备份 2 (904 × 96, top=348, bg #242426)       ← 默认态

每个列表项内部（左→右）：
├── 缩略图区  (128 × 72, bg #d8d8d8, 圆角 8)
│   └── 视频封面位图（被 alpha mask 裁圆角）
├── 平台 logo 圆形 (32 × 32, top: 行内偏上)
│   ├── 圆形底（位图）
│   └── 平台图标（如 Netflix N 字位图，叠在圆心）
├── 标题文字  Arial Regular 14 / line 20 / #eee
│   "This is the tittle of the Video"
├── 标签组 tag × 1-4 个（横向排列）
│   每个 tag: 边框 #fa8a03 (opacity 30%), 圆角 12, 文字 #fa8a03 Arial 12 / line 18
│   示例: 07:05 / 1080p / 17:68M / StreamFab
└── 右侧操作 icon_1
    ├── icon_file（打开文件夹）
    └── icon_close（删除/关闭）
```

## 关键样式 token

| 用途 | 值 |
|---|---|
| 列表容器背景 | `#242426` |
| 列表项默认背景 | `#242426` |
| 列表项 hover/选中背景 | `#2a2a2c` |
| 容器圆角 | 8 |
| 列表项分隔 | 无显式分隔线，靠行 bg 区别 |
| 缩略图占位背景 | `#d8d8d8`（资源未加载时） |
| 缩略图圆角 | 8 |
| 缩略图尺寸 | 128 × 72（16:9） |
| 平台 logo 尺寸 | 圆形 ~32 × 32 |
| 标题文字 | `#eee`  Arial 14 / line 20 |
| 标签描边 | `#fa8a03` border @ opacity 30% |
| 标签文字 | `#fa8a03`  Arial 12 / line 18 |
| 标签圆角 | 12 |
| 标签内间距 | 横向 ~10px，纵向 ~3px（按视觉估算） |
| 行高度 | 96 |
| 行内水平内边距（左→缩略图） | ~16px (1.77%) |
| 行内水平内边距（右→操作图标） | ~16px (1.77%) |
| 操作图标行内位置 | 垂直居中 (inset_top=41.67%, inset_bottom=41.67%) |

## 标签 tag 语义推断

| 内容 | 含义 |
|---|---|
| `07:05` | 时长 (mm:ss) |
| `1080p` | 分辨率 |
| `17:68M` | 文件大小（疑似排版误差，应为 `17.68M`） |
| `StreamFab` | 来源 / 录制工具品牌标识 |

注：第 2 行（hover 态）缺少 `StreamFab` 标签，说明 tag 数量是可变的，应按数据动态渲染。

## 图标 / 图片资源

- 不缓存远程 URL，不主动下载
- 涉及图标（仅记名）：`icon_file`、`icon_close`、平台 logo 圆形底、平台图标（如 Netflix）、视频封面缩略图、缩略图 alpha mask
- 实现优先用：`lucide-react`（Folder / X 图标）替代 icon_file / icon_close；平台 logo 用本地小图或文字占位；视频缩略图按业务数据 mock

## 业务语义

- 这是 "My Files" 路由（`/files`）的主区域，对应 sidebar 中带红色 badge "3" 的入口
- 列表行数据：缩略图 + 平台来源 + 标题 + 元信息标签组 + 行操作
- 行操作：`打开文件夹位置` 和 `删除记录`

## 与当前 FilesPage 实现的差异（src/pages/FilesPage.tsx）

待落地代码时再核对；本摘要仅供后续实现时对齐设计 token。

## 不在本节点的内容

- My Files 页的顶部筛选 / 排序 / 全选条（设计稿未提供）
- 空列表态、加载态
- 选中多选态、批量删除（未提供）
- 浏览器壳（沿用 0:1367）
